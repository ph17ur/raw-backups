# raw-backups
A simple Bash script for photographers who shoot RAW (or RAW+JPG) and want an easy way to back up their CR2 RAW files — and their Darktable/Lightroom .xmp sidecar files — to a separate "cold storage" location, without disturbing the local working folder structure.
